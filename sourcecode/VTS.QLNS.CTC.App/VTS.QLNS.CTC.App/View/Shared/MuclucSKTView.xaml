<UserControl x:Class="VTS.QLNS.CTC.App.View.Shared.MuclucSKTView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:VTS.QLNS.CTC.App.View.Shared"
             xmlns:extensions="clr-namespace:VTS.QLNS.CTC.App.Extensions"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:component="clr-namespace:VTS.QLNS.CTC.App.Component"
             xmlns:helper="clr-namespace:VTS.QLNS.CTC.App.Helper"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:wpf="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="800">
    <UserControl.InputBindings>
    </UserControl.InputBindings>
    <Grid x:Name="pageControlCustom">
        <Grid.InputBindings>
            <KeyBinding Key="F5" Command="{Binding RefreshCommand}" CommandParameter="{Binding ElementName=dgdDataMLSKT, Path=.}"/>
            <KeyBinding Key="F3" Command="{Binding AddCommand}" CommandParameter="{Binding ElementName=dgdDataMLSKT, Path=.}"/>
            <KeyBinding Key="F4" Command="{Binding DeleteCommand}" CommandParameter="{Binding ElementName=dgdDataMLSKT, Path=.}"/>
            <KeyBinding Key="F10" Command="{Binding SaveCommand}" CommandParameter="{Binding ElementName=dgdDataMLSKT, Path=.}"/>
            <KeyBinding Key="F1" Command="{Binding OpenFilterPopupCommand}" />
            <KeyBinding Key="F6" Command="{Binding OpenReferencePopupCommand}" CommandParameter="{Binding ElementName=dgdDataMLSKT, Path=.}" />
            <KeyBinding Key="F7" Command="{Binding ViewDetailCommand}" CommandParameter="{Binding ElementName=dgdDataMLSKT, Path=.}" />
            <KeyBinding Key="LeftAlt" Command="{Binding SaveReferenceCommand}" />
            <KeyBinding Gesture="Ctrl+F3" Command="{Binding AddChildCommand}" CommandParameter="{Binding ElementName=dgdDataMLSKT, Path=.}"/>
            <KeyBinding Gesture="Ctrl+F6" Command="{Binding SelectMlnsParentCommand}" CommandParameter="{Binding ElementName=dgdDataMLSKT, Path=.}"/>
        </Grid.InputBindings>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">
            <materialDesign:PackIcon Kind="DriveDocument" Height="32" Width="32" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
            <StackPanel Orientation="Vertical">
                <TextBlock FontWeight="Bold" Text="{Binding Title}"></TextBlock>
                <DockPanel>
                    <TextBlock Text="{Binding Description}"></TextBlock>
                    <TextBlock Text="{Binding Total}" HorizontalAlignment="Left"></TextBlock>
                    <TextBlock Text="{Binding Hint}" HorizontalAlignment="Right"></TextBlock>
                    <!--
                    <TextBlock Text="[F3] Thêm dòng, [F4] Xóa dòng, [F10] Lưu" HorizontalAlignment="Right" FontWeight="Medium"></TextBlock>
                    -->
                </DockPanel>
                <Separator Background="{StaticResource MaterialDesignDivider}" Margin="0 5 0 0"/>
            </StackPanel>
        </DockPanel>

        <ScrollViewer Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <StackPanel Margin="0 5" Orientation="Horizontal" Visibility="{Binding IsDialog, Converter={StaticResource InvertBooleanToVisibilityConverter}}">
                <StackPanel.Resources>
                    <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource MaterialDesignFloatingHintComboBox}">
                        <Setter Property="Width" Value="150"/>
                    </Style>
                </StackPanel.Resources>
                <Button Visibility="{Binding IsVisibleAddBtn, Converter={StaticResource BooleanToVisibilityConverter}}"  Style="{DynamicResource SearchBarBtnStyle}" Command="{Binding AddCommand}" CommandParameter="{Binding ElementName=dgdDataMLNS, Path=.}" HorizontalAlignment="Right"  x:Name="btnAdd" >
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Plus"/>
                        <TextBlock Style="{StaticResource SearchBarBtnTextStyle}" Text="Tạo Mới (F3)"/>
                    </StackPanel>
                </Button>
                <Separator Visibility="{Binding IsVisibleAddBtn, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{StaticResource SearchBarHorizontalDividerStyle}" Margin="5 7"/>
                <Button Visibility="{Binding IsEnableAddChild, Converter={StaticResource BooleanToVisibilityConverter}}"  Style="{DynamicResource SearchBarBtnStyle}" Command="{Binding AddChildCommand}" CommandParameter="{Binding ElementName=dgdDataMLNS, Path=.}" HorizontalAlignment="Right"  x:Name="btnAddChild" >
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Plus"/>
                        <TextBlock Style="{StaticResource SearchBarBtnTextStyle}" Text="Tạo Mới Dòng Con (Ctrl+F3)"/>
                    </StackPanel>
                </Button>
                <Separator Visibility="{Binding IsEnableAddChild, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{StaticResource SearchBarHorizontalDividerStyle}" Margin="5 7"/>
                <Button Visibility="{Binding IsVisibleDeleteBtn, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{DynamicResource SearchBarBtnStyle}" Command="{Binding DeleteCommand}" CommandParameter="{Binding ElementName=dgdDataMLNS, Path=.}" HorizontalAlignment="Right"  x:Name="btnDelete" >
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Delete"/>
                        <TextBlock Style="{StaticResource SearchBarBtnTextStyle}" Text="Xóa (F4)"/>
                    </StackPanel>
                </Button>
                <Separator Visibility="{Binding IsVisibleDeleteBtn, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{StaticResource SearchBarHorizontalDividerStyle}" Margin="5 7"/>
                <Button Visibility="{Binding IsVisibleSaveBtn, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{DynamicResource SearchBarBtnStyle}" Command="{Binding SaveCommand}" CommandParameter="{Binding ElementName=dgdDataMLNS, Path=.}" HorizontalAlignment="Right"  x:Name="btnSave" >
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ContentSave"/>
                        <TextBlock Style="{StaticResource SearchBarBtnTextStyle}" Text="Lưu dữ liệu (F10)"/>
                    </StackPanel>
                </Button>
                <Separator Visibility="{Binding IsVisibleSaveBtn, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{StaticResource SearchBarHorizontalDividerStyle}" Margin="5 7"/>
                <Button Visibility="{Binding IsVisibleRefreshBtn, Converter={StaticResource BooleanToVisibilityConverter}}"  Style="{DynamicResource SearchBarBtnStyle}" Command="{Binding RefreshCommand}" CommandParameter="{Binding ElementName=dgdDataMLNS, Path=.}"  HorizontalAlignment="Right"  x:Name="btnRefesh"  >
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Refresh"/>
                        <TextBlock Style="{StaticResource SearchBarBtnTextStyle}" Text="Refesh (F5)"/>
                    </StackPanel>
                </Button>
                <Separator Visibility="{Binding IsVisibleRefreshBtn, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{StaticResource SearchBarHorizontalDividerStyle}" Margin="5 7"/>
                <ComboBox Name="cbFrozenColumn"
                            materialDesign:HintAssist.Hint="Cố định cột"
                            DisplayMemberPath="DisplayItem"
                            SelectedValuePath="ValueItem"/>
                <Separator Visibility="{Binding IsVisibleToggleBtn, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                <Button x:Name="btnShowCol" Style="{StaticResource SearchBarBtnStyle}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="PlusMinusVariant" />
                        <TextBlock Style="{StaticResource SearchBarBtnTextStyle}" Text="Ẩn/hiện cột" />
                        <materialDesign:PopupBox
                                Name="pbDynamicColumn"
                                Margin="-10 0 0 0"
                                HorizontalAlignment="Right"
                                StaysOpen="True">
                            <materialDesign:PopupBox.ToggleContent>
                                <materialDesign:PackIcon Kind="MenuDown"/>
                            </materialDesign:PopupBox.ToggleContent>
                        </materialDesign:PopupBox>
                    </StackPanel>
                </Button>
                <Separator Visibility="{Binding IsVisibleToggleBtn, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{StaticResource SearchBarHorizontalDividerStyle}" Margin="5 7"/>
                <Button Visibility="{Binding ExportBtnVisibility}" x:Name="btnExcel" Style="{StaticResource SearchBarBtnStyle}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="FileExcel" />
                        <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">Excel</TextBlock>
                        <materialDesign:PopupBox
                        Margin="-10 0 0 0"
                        Grid.Column="1" 
                        HorizontalAlignment="Right"
                        IsPopupOpen="{Binding IsOpenExcelPopup, Mode=TwoWay}">
                            <materialDesign:PopupBox.ToggleContent>
                                <materialDesign:PackIcon Kind="MenuDown" />
                            </materialDesign:PopupBox.ToggleContent>
                            <StackPanel>
                                <Button Content="Xuất file excel" Command="{Binding ExportCommand}" IsEnabled="{Binding IsEnableExportExcel}"/>
                                <Button Content="Import từ file Excel" Command="{Binding ImportExcelCommand}" IsEnabled="{Binding IsEnableImportExcel}"/>
                            </StackPanel>
                        </materialDesign:PopupBox>
                    </StackPanel>
                </Button>
                <Separator Visibility="{Binding ExportBtnVisibility}" Style="{StaticResource SearchBarHorizontalDividerStyle}" Margin="5 7"/>
                <Button Visibility="{Binding IsVisibleViewDetailBtn, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{DynamicResource SearchBarBtnStyle}" Command="{Binding ViewDetailCommand}" HorizontalAlignment="Right" >
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Eye"/>
                        <TextBlock Style="{StaticResource SearchBarBtnTextStyle}" Text="Xem chi tiết (F7)"/>
                    </StackPanel>
                </Button>
                <Separator Visibility="{Binding IsVisibleFilterByMlnsMappingType, Converter={StaticResource BooleanToVisibilityConverter}}" Style="{StaticResource SearchBarHorizontalDividerStyle}" Margin="5 7"/>
                <Button Style="{DynamicResource SearchBarBtnStyle}" Command="{Binding SelectMlnsParentCommand}" HorizontalAlignment="Right" >
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CodeJson"/>
                        <TextBlock Style="{StaticResource SearchBarBtnTextStyle}" Text="Chọn mlskt cha (Ctrl + F6)"/>
                    </StackPanel>
                </Button>
                <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" Margin="5 7"/>
                <TextBlock VerticalAlignment="Center" Text=" Lọc theo cột (F1)"/>
            </StackPanel>
        </ScrollViewer>

        <component:CategoryExpandedDataGrid
            x:Name="dgdDataMLSKT"
            Grid.Row="2"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            BorderThickness="1"
            GridLinesVisibility="All"
            FrozenColumnCount="3"
            ElementFrozenColumn="{Binding ElementName=cbFrozenColumn, Path=., Mode=TwoWay}"
            ElementDynamicColumn="{Binding ElementName=pbDynamicColumn, Path=., Mode=TwoWay}"
            Style="{StaticResource MaterialDesignDataGrid}"
            CellStyle="{StaticResource GenericDataGridCellDetail}"
            RowStyle="{StaticResource GenericDataGridRowDetail}"
            SelectedItem="{Binding SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            ItemsSource="{Binding Items, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            extensions:GridAutoGeneratingColumnBehaviour.Command="{Binding AutoGeneratingColumnsCommand}"
            BeginningEdit="DataGrid_BeginningEdit"
            extensions:SelectedCellChangedProperty.Command="{Binding SelectedCellChangeCommand}"
            MaxWidth="2560"
            MaxHeight="1600"
            AutoGenerateColumns="False">
            <DataGrid.Resources>
                <helper:BindingProxy
                        x:Key="DataContextProxy"
                        Data="{Binding}" />
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Chọn" 
                                        Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                        ElementStyle="{StaticResource MaterialDesignDataGridCheckBoxColumnStyle}"
                                        EditingElementStyle="{StaticResource MaterialDesignDataGridCheckBoxColumnEditingStyle}"
                                        Visibility="{Binding Data.IsDialog, Source={StaticResource DataContextProxy}, Converter={StaticResource InverseBoolToVisConverter}}">

                </DataGridCheckBoxColumn>
                <materialDesign:DataGridTextColumn Binding="{Binding SL, UpdateSourceTrigger=PropertyChanged}" ElementStyle="{StaticResource DataGridTextColumnStyle}" EditingElementStyle="{StaticResource DataGridTextColumnEditingStyle}">
                    <materialDesign:DataGridTextColumn.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">Loại</TextBlock>
                            <materialDesign:PopupBox
                Name="SLpopupbox"
                Padding="0,0,0,0"
                HorizontalAlignment="Right">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="FilterOutline" Opacity="0.5"/>
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <materialDesign:ColorZone Mode="PrimaryLight">
                                        <Border Background="White" Margin="5,5,5,5" MaxHeight="30" CornerRadius="3" BorderThickness="1,1,1,1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBox Width="150" Margin="5,0,0,0" BorderThickness="0,0,0,0"
                                    Text="{Binding Data.FilterModel.SL, Source={StaticResource DataContextProxy}, UpdateSourceTrigger=PropertyChanged}"    
                                    VerticalAlignment="Center" Name="SLtxtbox" Loaded="Lnstxtbox_Loaded">
                                                    <TextBox.InputBindings>
                                                        <KeyBinding Command="{Binding Data.FilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SL" Key="Return" />
                                                        <KeyBinding Command="{Binding Data.EscapteFilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SL" Key="Esc" />
                                                    </TextBox.InputBindings>
                                                </TextBox>
                                                <Button Height="30" Width="30" Style="{StaticResource MaterialDesignToolButton}">
                                                    <materialDesign:PackIcon Kind="Filter"></materialDesign:PackIcon>
                                                </Button>
                                            </StackPanel>
                                        </Border>
                                    </materialDesign:ColorZone>
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </materialDesign:DataGridTextColumn.Header>
                </materialDesign:DataGridTextColumn>
                <materialDesign:DataGridTextColumn Binding="{Binding SK, UpdateSourceTrigger=PropertyChanged}" ElementStyle="{StaticResource DataGridTextColumnStyle}" EditingElementStyle="{StaticResource DataGridTextColumnEditingStyle}">
                    <materialDesign:DataGridTextColumn.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">Khoản</TextBlock>
                            <materialDesign:PopupBox
Name="SKpopupbox"
Padding="0,0,0,0"
HorizontalAlignment="Right">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="FilterOutline" Opacity="0.5"/>
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <materialDesign:ColorZone Mode="PrimaryLight">
                                        <Border Background="White" Margin="5,5,5,5" MaxHeight="30" CornerRadius="3" BorderThickness="1,1,1,1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBox Width="150" Margin="5,0,0,0" BorderThickness="0,0,0,0"
                    Text="{Binding Data.FilterModel.SK, Source={StaticResource DataContextProxy}, UpdateSourceTrigger=PropertyChanged}"    
                    VerticalAlignment="Center" Name="SKtxtbox" Loaded="Lnstxtbox_Loaded">
                                                    <TextBox.InputBindings>
                                                        <KeyBinding Command="{Binding Data.FilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SK" Key="Return" />
                                                        <KeyBinding Command="{Binding Data.EscapteFilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SK" Key="Esc" />
                                                    </TextBox.InputBindings>
                                                </TextBox>
                                                <Button Height="30" Width="30" Style="{StaticResource MaterialDesignToolButton}">
                                                    <materialDesign:PackIcon Kind="Filter"></materialDesign:PackIcon>
                                                </Button>
                                            </StackPanel>
                                        </Border>
                                    </materialDesign:ColorZone>
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </materialDesign:DataGridTextColumn.Header>
                </materialDesign:DataGridTextColumn>
                <materialDesign:DataGridTextColumn Binding="{Binding SM, UpdateSourceTrigger=PropertyChanged}" ElementStyle="{StaticResource DataGridTextColumnStyle}" EditingElementStyle="{StaticResource DataGridTextColumnEditingStyle}">
                    <materialDesign:DataGridTextColumn.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">Mục - Tiểu mục</TextBlock>
                            <materialDesign:PopupBox
                                Name="SMpopupbox"
                                Padding="0,0,0,0"
                                HorizontalAlignment="Right">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="FilterOutline" Opacity="0.5"/>
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <materialDesign:ColorZone Mode="PrimaryLight">
                                        <Border Background="White" Margin="5,5,5,5" MaxHeight="30" CornerRadius="3" BorderThickness="1,1,1,1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBox Width="150" Margin="5,0,0,0" BorderThickness="0,0,0,0"
                                                    Text="{Binding Data.FilterModel.SM, Source={StaticResource DataContextProxy}, UpdateSourceTrigger=PropertyChanged}"    
                                                    VerticalAlignment="Center" Name="SMtxtbox" Loaded="Lnstxtbox_Loaded">
                                                    <TextBox.InputBindings>
                                                        <KeyBinding Command="{Binding Data.FilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SM" Key="Return" />
                                                        <KeyBinding Command="{Binding Data.EscapteFilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SM" Key="Esc" />
                                                    </TextBox.InputBindings>
                                                </TextBox>
                                                <Button Height="30" Width="30" Style="{StaticResource MaterialDesignToolButton}">
                                                    <materialDesign:PackIcon Kind="Filter"></materialDesign:PackIcon>
                                                </Button>
                                            </StackPanel>
                                        </Border>
                                    </materialDesign:ColorZone>
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </materialDesign:DataGridTextColumn.Header>
                </materialDesign:DataGridTextColumn>
                <materialDesign:DataGridTextColumn IsReadOnly="True" Binding="{Binding SNGCha, UpdateSourceTrigger=PropertyChanged}" ElementStyle="{StaticResource DataGridTextColumnStyle}" EditingElementStyle="{StaticResource DataGridTextColumnEditingStyle}">
                    <materialDesign:DataGridTextColumn.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">Ngành cha (F6)</TextBlock>
                            <materialDesign:PopupBox
                                Name="SNGChapopupbox"
                                Padding="0,0,0,0"
                                HorizontalAlignment="Right">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="FilterOutline" Opacity="0.5"/>
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <materialDesign:ColorZone Mode="PrimaryLight">
                                        <Border Background="White" Margin="5,5,5,5" MaxHeight="30" CornerRadius="3" BorderThickness="1,1,1,1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBox Width="150" Margin="5,0,0,0" BorderThickness="0,0,0,0"
                                                    Text="{Binding Data.FilterModel.SNGCha, Source={StaticResource DataContextProxy}, UpdateSourceTrigger=PropertyChanged}"    
                                                    VerticalAlignment="Center" Name="SNGChatxtbox" Loaded="Lnstxtbox_Loaded">
                                                    <TextBox.InputBindings>
                                                        <KeyBinding Command="{Binding Data.FilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SNGCha" Key="Return" />
                                                        <KeyBinding Command="{Binding Data.EscapteFilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SNGCha" Key="Esc" />
                                                    </TextBox.InputBindings>
                                                </TextBox>
                                                <Button Height="30" Width="30" Style="{StaticResource MaterialDesignToolButton}">
                                                    <materialDesign:PackIcon Kind="FilterOutline" Background="White" Opacity="0.5"></materialDesign:PackIcon>
                                                </Button>
                                            </StackPanel>
                                        </Border>
                                    </materialDesign:ColorZone>
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </materialDesign:DataGridTextColumn.Header>
                </materialDesign:DataGridTextColumn>
                <materialDesign:DataGridTextColumn Binding="{Binding SNg, UpdateSourceTrigger=PropertyChanged}" ElementStyle="{StaticResource DataGridTextColumnStyle}" EditingElementStyle="{StaticResource DataGridTextColumnEditingStyle}">
                    <materialDesign:DataGridTextColumn.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">Ngành chi tiết</TextBlock>
                            <materialDesign:PopupBox
                                Name="SNgpopupbox"
                                Padding="0,0,0,0"
                                HorizontalAlignment="Right">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="FilterOutline" Opacity="0.5"/>
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <materialDesign:ColorZone Mode="PrimaryLight">
                                        <Border Background="White" Margin="5,5,5,5" MaxHeight="30" CornerRadius="3" BorderThickness="1,1,1,1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBox Width="150" Margin="5,0,0,0" BorderThickness="0,0,0,0"
                                                    Text="{Binding Data.FilterModel.SNg, Source={StaticResource DataContextProxy}, UpdateSourceTrigger=PropertyChanged}"    
                                                    VerticalAlignment="Center" Name="SNgtxtbox" Loaded="Lnstxtbox_Loaded">
                                                    <TextBox.InputBindings>
                                                        <KeyBinding Command="{Binding Data.FilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SNg" Key="Return" />
                                                        <KeyBinding Command="{Binding Data.EscapteFilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SNg" Key="Esc" />
                                                    </TextBox.InputBindings>
                                                </TextBox>
                                                <Button Height="30" Width="30" Style="{StaticResource MaterialDesignToolButton}">
                                                    <materialDesign:PackIcon Kind="Filter" Background="White"></materialDesign:PackIcon>
                                                </Button>
                                            </StackPanel>
                                        </Border>
                                    </materialDesign:ColorZone>
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </materialDesign:DataGridTextColumn.Header>
                </materialDesign:DataGridTextColumn>
                <materialDesign:DataGridTextColumn Binding="{Binding SSTT, UpdateSourceTrigger=PropertyChanged}" ElementStyle="{StaticResource DataGridTextColumnStyle}" EditingElementStyle="{StaticResource DataGridTextColumnEditingStyle}">
                    <materialDesign:DataGridTextColumn.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">STT</TextBlock>
                            <materialDesign:PopupBox
                                Name="SSTTpopupbox"
                                Padding="0,0,0,0"
                                HorizontalAlignment="Right">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="FilterOutline" Opacity="0.5"/>
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <materialDesign:ColorZone Mode="PrimaryLight">
                                        <Border Background="White" Margin="5,5,5,5" MaxHeight="30" CornerRadius="3" BorderThickness="1,1,1,1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBox Width="150" Margin="5,0,0,0" BorderThickness="0,0,0,0"
                                                    Text="{Binding Data.FilterModel.SSTT, Source={StaticResource DataContextProxy}, UpdateSourceTrigger=PropertyChanged}"    
                                                    VerticalAlignment="Center" Name="SSTTtxtbox" Loaded="Lnstxtbox_Loaded">
                                                    <TextBox.InputBindings>
                                                        <KeyBinding Command="{Binding Data.FilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SSTT" Key="Return" />
                                                        <KeyBinding Command="{Binding Data.EscapteFilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SSTT" Key="Esc" />
                                                    </TextBox.InputBindings>
                                                </TextBox>
                                                <Button Height="30" Width="30" Style="{StaticResource MaterialDesignToolButton}">
                                                    <materialDesign:PackIcon Kind="Filter" Background="White"></materialDesign:PackIcon>
                                                </Button>
                                            </StackPanel>
                                        </Border>
                                    </materialDesign:ColorZone>
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </materialDesign:DataGridTextColumn.Header>
                </materialDesign:DataGridTextColumn>
                <materialDesign:DataGridTextColumn Binding="{Binding SSttBC, UpdateSourceTrigger=PropertyChanged}" ElementStyle="{StaticResource DataGridTextColumnStyle}" Visibility="{Binding Data.TM, Source={StaticResource DataContextProxy}, Converter={StaticResource BooleanToVisibilityConverter}}" EditingElementStyle="{StaticResource DataGridTextColumnEditingStyle}">
                    <materialDesign:DataGridTextColumn.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">STT in báo cáo</TextBlock>
                            <materialDesign:PopupBox
                                Name="SSttBCpopupbox"
                                Padding="0,0,0,0"
                                HorizontalAlignment="Right">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="FilterOutline" Opacity="0.5"/>
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <materialDesign:ColorZone Mode="PrimaryLight">
                                        <Border Background="White" Margin="5,5,5,5" MaxHeight="30" CornerRadius="3" BorderThickness="1,1,1,1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBox Width="150" Margin="5,0,0,0" BorderThickness="0,0,0,0"
                                                    Text="{Binding Data.FilterModel.SSttBC, Source={StaticResource DataContextProxy}, UpdateSourceTrigger=PropertyChanged}"    
                                                    VerticalAlignment="Center" Name="SSttBCtxtbox" Loaded="Lnstxtbox_Loaded">
                                                    <TextBox.InputBindings>
                                                        <KeyBinding Command="{Binding Data.FilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SSttBC" Key="Return" />
                                                        <KeyBinding Command="{Binding Data.EscapteFilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SSttBC" Key="Esc" />
                                                    </TextBox.InputBindings>
                                                </TextBox>
                                                <Button Height="30" Width="30" Style="{StaticResource MaterialDesignToolButton}">
                                                    <materialDesign:PackIcon Kind="Filter" Background="White"></materialDesign:PackIcon>
                                                </Button>
                                            </StackPanel>
                                        </Border>
                                    </materialDesign:ColorZone>
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </materialDesign:DataGridTextColumn.Header>
                </materialDesign:DataGridTextColumn>
                <materialDesign:DataGridTextColumn Binding="{Binding SKyHieu, UpdateSourceTrigger=PropertyChanged}" ElementStyle="{StaticResource DataGridTextColumnStyle}" EditingElementStyle="{StaticResource DataGridTextColumnEditingStyle}">
                    <materialDesign:DataGridTextColumn.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">Mã</TextBlock>
                            <materialDesign:PopupBox
                                Name="SKyHieupopupbox"
                                Padding="0,0,0,0"
                                HorizontalAlignment="Right">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="FilterOutline" Opacity="0.5"/>
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <materialDesign:ColorZone Mode="PrimaryLight">
                                        <Border Background="White" Margin="5,5,5,5" MaxHeight="30" CornerRadius="3" BorderThickness="1,1,1,1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBox Width="150" Margin="5,0,0,0" BorderThickness="0,0,0,0"
                                                    Text="{Binding Data.FilterModel.SKyHieu, Source={StaticResource DataContextProxy}, UpdateSourceTrigger=PropertyChanged}"    
                                                    VerticalAlignment="Center" Name="SKyHieutxtbox" Loaded="Lnstxtbox_Loaded">
                                                    <TextBox.InputBindings>
                                                        <KeyBinding Command="{Binding Data.FilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SKyHieu" Key="Return" />
                                                        <KeyBinding Command="{Binding Data.EscapteFilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SKyHieu" Key="Esc" />
                                                    </TextBox.InputBindings>
                                                </TextBox>
                                                <Button Height="30" Width="30" Style="{StaticResource MaterialDesignToolButton}">
                                                    <materialDesign:PackIcon Kind="Filter" Background="White"></materialDesign:PackIcon>
                                                </Button>
                                            </StackPanel>
                                        </Border>
                                    </materialDesign:ColorZone>
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </materialDesign:DataGridTextColumn.Header>
                </materialDesign:DataGridTextColumn>                
                <materialDesign:DataGridTextColumn Binding="{Binding SLoaiNhap, UpdateSourceTrigger=PropertyChanged}" ElementStyle="{StaticResource DataGridTextColumnStyle}" EditingElementStyle="{StaticResource DataGridTextColumnEditingStyle}">
                    <materialDesign:DataGridTextColumn.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">Loại nhập</TextBlock>
                            <materialDesign:PopupBox
                                Name="SLoaiNhappopupbox"
                                Padding="0,0,0,0"
                                HorizontalAlignment="Right">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="FilterOutline" Opacity="0.5"/>
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <materialDesign:ColorZone Mode="PrimaryLight">
                                        <Border Background="White" Margin="5,5,5,5" MaxHeight="30" CornerRadius="3" BorderThickness="1,1,1,1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBox Width="150" Margin="5,0,0,0" BorderThickness="0,0,0,0"
                                                    Text="{Binding Data.FilterModel.SLoaiNhap, Source={StaticResource DataContextProxy}, UpdateSourceTrigger=PropertyChanged}"    
                                                    VerticalAlignment="Center" Name="SLoaiNhaptxtbox" Loaded="Lnstxtbox_Loaded">
                                                    <TextBox.InputBindings>
                                                        <KeyBinding Command="{Binding Data.FilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SLoaiNhap" Key="Return" />
                                                        <KeyBinding Command="{Binding Data.EscapteFilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SLoaiNhap" Key="Esc" />
                                                    </TextBox.InputBindings>
                                                </TextBox>
                                                <Button Height="30" Width="30" Style="{StaticResource MaterialDesignToolButton}">
                                                    <materialDesign:PackIcon Kind="Filter" Background="White"></materialDesign:PackIcon>
                                                </Button>
                                            </StackPanel>
                                        </Border>
                                    </materialDesign:ColorZone>
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </materialDesign:DataGridTextColumn.Header>
                </materialDesign:DataGridTextColumn>
                <materialDesign:DataGridTextColumn Binding="{Binding SMoTa, UpdateSourceTrigger=PropertyChanged}" ElementStyle="{StaticResource DataGridTextColumnStyle}" EditingElementStyle="{StaticResource DataGridTextColumnEditingStyle}">
                    <materialDesign:DataGridTextColumn.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">Mô tả</TextBlock>
                            <materialDesign:PopupBox
                                Name="SMoTapopupbox"
                                Padding="0,0,0,0"
                                HorizontalAlignment="Right">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="FilterOutline" Opacity="0.5"/>
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <materialDesign:ColorZone Mode="PrimaryLight">
                                        <Border Background="White" Margin="5,5,5,5" MaxHeight="30" CornerRadius="3" BorderThickness="1,1,1,1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBox Width="150" Margin="5,0,0,0" BorderThickness="0,0,0,0"
                                                    Text="{Binding Data.FilterModel.SMoTa, Source={StaticResource DataContextProxy}, UpdateSourceTrigger=PropertyChanged}"    
                                                    VerticalAlignment="Center" Name="SMoTatxtbox" Loaded="Lnstxtbox_Loaded">
                                                    <TextBox.InputBindings>
                                                        <KeyBinding Command="{Binding Data.FilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SMoTa" Key="Return" />
                                                        <KeyBinding Command="{Binding Data.EscapteFilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SMoTa" Key="Esc" />
                                                    </TextBox.InputBindings>
                                                </TextBox>
                                                <Button Height="30" Width="30" Style="{StaticResource MaterialDesignToolButton}">
                                                    <materialDesign:PackIcon Kind="FilterOutline" Background="White" Opacity="0.5"></materialDesign:PackIcon>
                                                </Button>
                                            </StackPanel>
                                        </Border>
                                    </materialDesign:ColorZone>
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </materialDesign:DataGridTextColumn.Header>
                </materialDesign:DataGridTextColumn>
                <materialDesign:DataGridTextColumn Binding="{Binding SKyHieuCu, UpdateSourceTrigger=PropertyChanged}" ElementStyle="{StaticResource DataGridTextColumnStyle}" EditingElementStyle="{StaticResource DataGridTextColumnEditingStyle}">
                    <materialDesign:DataGridTextColumn.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">Mã mục lục năm trước</TextBlock>
                            <materialDesign:PopupBox
                Name="SKyHieuCupopupbox"
                Padding="0,0,0,0"
                HorizontalAlignment="Right">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="FilterOutline" Opacity="0.5"/>
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <materialDesign:ColorZone Mode="PrimaryLight">
                                        <Border Background="White" Margin="5,5,5,5" MaxHeight="30" CornerRadius="3" BorderThickness="1,1,1,1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBox Width="150" Margin="5,0,0,0" BorderThickness="0,0,0,0"
                                    Text="{Binding Data.FilterModel.SKyHieuCu, Source={StaticResource DataContextProxy}, UpdateSourceTrigger=PropertyChanged}"    
                                    VerticalAlignment="Center" Name="SKyHieuCutxtbox" Loaded="Lnstxtbox_Loaded">
                                                    <TextBox.InputBindings>
                                                        <KeyBinding Command="{Binding Data.FilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SKyHieuCu" Key="Return" />
                                                        <KeyBinding Command="{Binding Data.EscapteFilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="SKyHieuCu" Key="Esc" />
                                                    </TextBox.InputBindings>
                                                </TextBox>
                                                <Button Height="30" Width="30" Style="{StaticResource MaterialDesignToolButton}">
                                                    <materialDesign:PackIcon Kind="Filter" Background="White"></materialDesign:PackIcon>
                                                </Button>
                                            </StackPanel>
                                        </Border>
                                    </materialDesign:ColorZone>
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </materialDesign:DataGridTextColumn.Header>
                </materialDesign:DataGridTextColumn>
                <materialDesign:DataGridTextColumn IsReadOnly="True" Binding="{Binding KyHieuCha, UpdateSourceTrigger=PropertyChanged}" ElementStyle="{StaticResource DataGridTextColumnStyle}" EditingElementStyle="{StaticResource DataGridTextColumnEditingStyle}">
                    <materialDesign:DataGridTextColumn.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">Ký hiệu Cha (F6)</TextBlock>
                            <materialDesign:PopupBox
                                Name="KyHieuChapopupbox"
                                Padding="0,0,0,0"
                                HorizontalAlignment="Right">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="FilterOutline" Opacity="0.5"/>
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <materialDesign:ColorZone Mode="PrimaryLight">
                                        <Border Background="White" Margin="5,5,5,5" MaxHeight="30" CornerRadius="3" BorderThickness="1,1,1,1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBox Width="150" Margin="5,0,0,0" BorderThickness="0,0,0,0"
                                                    Text="{Binding Data.FilterModel.KyHieuCha, Source={StaticResource DataContextProxy}, UpdateSourceTrigger=PropertyChanged}"    
                                                    VerticalAlignment="Center" Name="MlnsParentNametxtbox" Loaded="Lnstxtbox_Loaded">
                                                    <TextBox.InputBindings>
                                                        <KeyBinding Command="{Binding Data.FilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="KyHieuCha" Key="Return" />
                                                        <KeyBinding Command="{Binding Data.EscapteFilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="KyHieuCha" Key="Esc" />
                                                    </TextBox.InputBindings>
                                                </TextBox>
                                                <Button Height="30" Width="30" Style="{StaticResource MaterialDesignToolButton}">
                                                    <materialDesign:PackIcon Kind="FilterOutline" Background="White" Opacity="0.5"></materialDesign:PackIcon>
                                                </Button>
                                            </StackPanel>
                                        </Border>
                                    </materialDesign:ColorZone>
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </materialDesign:DataGridTextColumn.Header>
                </materialDesign:DataGridTextColumn>
                <materialDesign:DataGridTextColumn IsReadOnly="True" Width="500" Binding="{Binding MLNS, UpdateSourceTrigger=PropertyChanged}" ElementStyle="{StaticResource DataGridTextColumnStyle}" EditingElementStyle="{StaticResource DataGridTextColumnEditingStyle}">
                    <materialDesign:DataGridTextColumn.Header>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">MLNS (F6)</TextBlock>
                            <materialDesign:PopupBox
                                Name="MLNSpopupbox"
                                Padding="0,0,0,0"
                                HorizontalAlignment="Right">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="FilterOutline" Opacity="0.5"/>
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                                    <materialDesign:ColorZone Mode="PrimaryLight">
                                        <Border Background="White" Margin="5,5,5,5" MaxHeight="30" CornerRadius="3" BorderThickness="1,1,1,1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBox Width="150" Margin="5,0,0,0" BorderThickness="0,0,0,0"
                                                    Text="{Binding Data.FilterModel.KyHieuCha, Source={StaticResource DataContextProxy}, UpdateSourceTrigger=PropertyChanged}"    
                                                    VerticalAlignment="Center" Name="MLNStxtbox" Loaded="Lnstxtbox_Loaded">
                                                    <TextBox.InputBindings>
                                                        <KeyBinding Command="{Binding Data.FilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="MLNS" Key="Return" />
                                                        <KeyBinding Command="{Binding Data.EscapteFilterCommand, Source={StaticResource DataContextProxy}}" CommandParameter="MLNS" Key="Esc" />
                                                    </TextBox.InputBindings>
                                                </TextBox>
                                                <Button Height="30" Width="30" Style="{StaticResource MaterialDesignToolButton}">
                                                    <materialDesign:PackIcon Kind="FilterOutline" Background="White" Opacity="0.5"></materialDesign:PackIcon>
                                                </Button>
                                            </StackPanel>
                                        </Border>
                                    </materialDesign:ColorZone>
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </materialDesign:DataGridTextColumn.Header>
                </materialDesign:DataGridTextColumn>
                <DataGridCheckBoxColumn Header="Có định mức"
                                        Binding="{Binding BCoDinhMuc, UpdateSourceTrigger=PropertyChanged}"
                                        ElementStyle="{StaticResource MaterialDesignDataGridCheckBoxColumnStyle}"
                                        EditingElementStyle="{StaticResource MaterialDesignDataGridCheckBoxColumnEditingStyle}"
                                        Visibility="{Binding Data.IsDialog, Source={StaticResource DataContextProxy}, Converter={StaticResource InverseBoolToVisConverter}}">
                </DataGridCheckBoxColumn>
            </DataGrid.Columns>
        </component:CategoryExpandedDataGrid>
        <StackPanel Orientation="Horizontal" Margin="0 15 15 15" HorizontalAlignment="Right" Visibility="{Binding IsDialog,Converter= {StaticResource BooleanToVisibilityConverter}}" Grid.Row="3">
            <Button 
                IsDefault="True"
                Margin="5 0 0 0"
                Command="{Binding SaveReferenceCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="ContentSave"/>
                    <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">Chọn (Alt+Enter)</TextBlock>
                </StackPanel>
            </Button>
            <Button
                Margin="5 0 0 0"
                IsCancel="True"
                Style="{StaticResource MaterialDesignOutlinedButton}"
                Command="{x:Static wpf:DialogHost.CloseDialogCommand}">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="CloseOutline" />
                    <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">Đóng</TextBlock>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
