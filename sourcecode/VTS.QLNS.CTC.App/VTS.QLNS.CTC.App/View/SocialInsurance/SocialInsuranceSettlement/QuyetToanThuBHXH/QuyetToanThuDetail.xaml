<Window x:Class="VTS.QLNS.CTC.App.View.SocialInsurance.SocialInsuranceSettlement.QuyetToanThuBHXH.QuyetToanThuDetail"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wpf="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
        xmlns:localEnum="clr-namespace:VTS.QLNS.CTC.Utility.Enum;assembly=VTS.QLNS.CTC.Utility"
        xmlns:localEnumSystem="clr-namespace:VTS.QLNS.CTC.Utility;assembly=VTS.QLNS.CTC.Utility"
        xmlns:extensions="clr-namespace:VTS.QLNS.CTC.App.Extensions"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:compt="clr-namespace:VTS.QLNS.CTC.App.Component"
        xmlns:dc="clr-namespace:VTS.QLNS.CTC.App.ViewModel.SocialInsurance.SocialInsuranceSettlement.QuyetToanThuBHXH"
        xmlns:helper="clr-namespace:VTS.QLNS.CTC.App.Helper"
        x:Name="DetailWindow"
        d:DataContext="{d:DesignInstance dc:QuyetToanThuDetailViewModel, IsDesignTimeCreatable=True}"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Quản lý thu chi bảo hiểm - QT thu BHXH, BHYT, BHTN"
        Height="768"
        Width="1280"
        WindowStartupLocation="CenterScreen"
        xmlns:ext="clr-namespace:VTS.QLNS.CTC.App.Extensions"
        WindowState="Maximized"
        Icon="/favicon.ico">
    <Window.InputBindings>
        <!--<KeyBinding Key="F4"
                    Command="{Binding DeleteCommand}"
                    CommandParameter="{Binding ElementName=DgQttVoucherDetail, Path=.}" />-->
        <KeyBinding Key="F10"
                    Command="{Binding SaveCommand}" />
        <KeyBinding Key="F5"
                    Command="{Binding RefreshCommand}" />
    </Window.InputBindings>
    <Window.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="groupingHeaderTemplate">
                <TextBlock Text="test"
                           Foreground="Navy"
                           FontWeight="Bold"
                           FontSize="12" />
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>
    <materialDesign:DialogHost Identifier="{x:Static localEnumSystem:SystemConstants.DETAIL_DIALOG}"
                               DialogTheme="Inherit">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0"
                        Orientation="Vertical">
                <DockPanel>
                    <materialDesign:PackIcon Kind="Dollar"
                                             Style="{StaticResource DialogScreenIconStyle}" />
                    <StackPanel Orientation="Vertical"
                                VerticalAlignment="Center">
                        <TextBlock Text="CHI TIẾT"
                                   FontWeight="Bold" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="Số: {0} - Ngày: {1} | {2}">
                                        <Binding Path="Model.SSoChungTu" />
                                        <Binding Path="Model.DNgayChungTu"
                                                 Converter="{StaticResource DateTimeToStringConverter}" />
                                        <Binding Path="Model.STenDonVi" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Grid>
                    </StackPanel>
                </DockPanel>
                <Separator Background="{DynamicResource MaterialDesignDivider}" />
            </StackPanel>
            <StackPanel Grid.Row="1">
                <StackPanel Grid.Row="1"
                            Orientation="Horizontal">

                    <Button Style="{StaticResource SearchBarBtnStyle}"
                            Command="{Binding SaveCommand}"
                            IsEnabled="{Binding IsSaveData}"
                            ToolTip="Lưu dữ liệu">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ContentSave" />
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}"
                                       Text="Lưu dữ liệu (F10)" />
                        </StackPanel>
                    </Button>
                    <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                    <Button ext:Permission.Identifier="BUDGET_SETTLEMENT_REGULARBUDGET_DETAIL_BTN_EXPLAIN"
                            Style="{StaticResource SearchBarBtnStyle}"
                            Command="{Binding ExplainCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="NoteText" />
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}"
                                       Text="Giải thích" />
                            <materialDesign:PopupBox Margin="-10 0 0 0"
                                                     Grid.Column="1"
                                                     HorizontalAlignment="Right"
                                                     IsPopupOpen="{Binding IsOpenExplainPopup, Mode=TwoWay}">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="MenuDown" />
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel>
                                    <Button
                                            Command="{Binding GiaiThichSoLieuCommand}"
                                            Content="Giải thích số liệu" />
                                    <Separator Visibility="{Binding IsShowNumberExplanation, Converter={StaticResource BooleanToVisibilityConverter}}"
                                               Style="{StaticResource MaterialDesignLightSeparator}"
                                               Background="Gray"
                                               Margin="20 0 20 0" />
                                    <Button Visibility="{Binding IsShowNumberExplanation, Converter={StaticResource InverseBoolToVisConverter}}"
                                            Command="{Binding GiaiThichBangLoiCommand}"
                                            Content="Giải thích bằng lời" />
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </Button>
                    <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                    <Button Style="{StaticResource SearchBarBtnStyle}"
                            ToolTip="Refresh"
                            Command="{Binding RefreshCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Refresh" />
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}"
                                       Text="Tải lại (F5)" />
                        </StackPanel>
                    </Button>
                    <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                    <Button x:Name="btnPrint"
                            Command="{Binding PrintCommand}"
                            Style="{StaticResource SearchBarBtnStyle}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Printer" />
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">In thông tri</TextBlock>
                        </StackPanel>
                    </Button>
                    <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}"
                               Visibility="{Binding IsVisibilityGetSalaryBudget, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <Button Style="{StaticResource SearchBarBtnStyle}"
                            Visibility="{Binding IsVisibilityGetSalaryBudget, Converter={StaticResource BooleanToVisibilityConverter}}"
                            IsEnabled="{Binding IsIsEnableGetSalaryBudget}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ApplicationImport" />
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}"
                                       Margin="5 0 5 0"
                                       Text="Lấy dữ liệu quỹ lương căn cứ" />
                            <materialDesign:PopupBox Margin="-10,0,0,0"
                                                     HorizontalAlignment="Right"
                                                     IsPopupOpen="{Binding IsOpenPrintPopup, Mode=TwoWay}">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="MenuDown" />
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel>
                                    <Button Content="1. Lấy từ lương"
                                            Command="{Binding GetBaseSalaryCommand}" />
                                    <Separator Style="{StaticResource MaterialDesignLightSeparator}"
                                               Background="Gray"
                                               Margin="20 0 20 0" />
                                    <Button Content="2. Lấy từ ngân sách"
                                            Command="{Binding GetBaseBudgetCommand}" />
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </Button>
                    <StackPanel Orientation="Horizontal"
                                Visibility="{Binding IsVoucherSummary, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                        <ComboBox materialDesign:HintAssist.Hint="-- Cách hiển thị dữ liệu --"
                                  DisplayMemberPath="DisplayItem"
                                  SelectedValuePath="ValueItem"
                                  ItemsSource="{Binding TypeShowAgencyKHT}"
                                  SelectedItem="{Binding SelectedTypeShowAgencyKHT, UpdateSourceTrigger=PropertyChanged}"
                                  Style="{StaticResource MaterialDesignFloatingHintComboBox}" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal"
                                Visibility="{Binding IsShowAgencyFilter, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                        <ComboBox materialDesign:HintAssist.Hint="-- Đơn vị --"
                                  materialDesign:TextFieldAssist.HasClearButton="True"
                                  DisplayMemberPath="DisplayItem"
                                  SelectedItem="{Binding SelectedAgency}"
                                  ItemsSource="{Binding Agencies}"
                                  Style="{StaticResource SearchBarHorizontalComboBoxStyle}" />
                    </StackPanel>

                    <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                    <Button x:Name="btnShowCol"
                            Style="{StaticResource SearchBarBtnStyle}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="PlusMinusVariant" />
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}"
                                       Text="Ẩn/hiện cột" />
                            <materialDesign:PopupBox Name="pbDynamicColumn"
                                                     Margin="-10 0 0 0"
                                                     HorizontalAlignment="Right"
                                                     StaysOpen="True">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="MenuDown" />
                                </materialDesign:PopupBox.ToggleContent>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </Button>
                    <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                    <ComboBox Name="cbFrozenColumn"
                              materialDesign:HintAssist.Hint="Cố định cột"
                              DisplayMemberPath="DisplayItem"
                              SelectedValuePath="ValueItem"
                              Style="{StaticResource MaterialDesignFloatingHintComboBox}" />
                    <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                    <Button Margin=" 0 5 0 0"
                            Command="{Binding SearchCommand}"
                            Style="{StaticResource SearchBarBtnStyle}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Search" />
                            <TextBlock Padding="8,0,0,0"
                                       Style="{StaticResource SearchBarBtnTextStyle}"
                                       Text="Tìm kiếm" />
                        </StackPanel>
                    </Button>
                    <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                    <Button Command="{Binding CloseCommand}"
                            CommandParameter="{Binding ElementName=DetailWindow}"
                            Style="{StaticResource SearchBarBtnStyle}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CloseOutline" />
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}"
                                       Text="Đóng" />
                        </StackPanel>
                    </Button>
                </StackPanel>
                <Separator Background="{DynamicResource MaterialDesignDivider}" />
                <StackPanel Orientation="Horizontal">
                    <Grid Width="{Binding ElementName=STenBhMLNS, Path=ActualWidth}"
                          Visibility="{Binding ElementName=MoTa, Path=Visibility}">
                        <TextBox x:Name="txtSelectedLNS"
                                 Margin="0 0 5 0"
                                 materialDesign:HintAssist.Hint="Nội dung"
                                 Text="{Binding SNoiDungSearch, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource MaterialDesignFloatingHintTextBox}">
                            <TextBox.InputBindings>
                                <KeyBinding Command="{Binding SearchCommand}"
                                            Key="Return" />
                            </TextBox.InputBindings>
                        </TextBox>
                        <materialDesign:PopupBox Margin="-10 10 0 0"
                                                 Grid.Column="1"
                                                 HorizontalAlignment="Right"
                                                 IsPopupOpen="{Binding IsPopupOpen, Mode=TwoWay}"
                                                 StaysOpen="True">
                            <materialDesign:PopupBox.ToggleContent>
                                <materialDesign:PackIcon Kind="MenuDown" />
                            </materialDesign:PopupBox.ToggleContent>
                            <StackPanel Margin="10"
                                        Orientation="Vertical">
                                <TextBox materialDesign:HintAssist.Hint="Tìm kiếm"
                                         Text="{Binding SearchLNS, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                                <DataGrid Height="450"
                                          Margin="0 10 0 0"
                                          VerticalAlignment="Top"
                                          AutoGenerateColumns="False"
                                          CanUserResizeColumns="True"
                                          BorderThickness="1"
                                          HorizontalScrollBarVisibility="Auto"
                                          SelectedItem="{Binding SelectedPopupItem}"
                                          ItemsSource="{Binding DataPopupSearchItems}"
                                          CanUserAddRows="False"
                                          RowStyle="{StaticResource DataGridRowDetail}">
                                    <DataGrid.Columns>
                                        <!--<DataGridTextColumn Header="LNS" Binding="{Binding Lns}" IsReadOnly="True"/>-->
                                        <DataGridTextColumn Header="Nội dung"
                                                            Binding="{Binding STenBhMLNS}"
                                                            IsReadOnly="True" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </StackPanel>
                        </materialDesign:PopupBox>
                    </Grid>
                    <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}"
                               Margin="0 0 0 0" />
                    <Button Style="{StaticResource SearchBarBtnStyle}"
                            Command="{Binding SearchCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Search" />
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">Tìm kiếm</TextBlock>
                        </StackPanel>
                    </Button>
                    <Button ext:CellFocusExtension.ElementToFocus="{Binding ElementName=txtSelectedLNS}"
                            Command="{Binding ClearSearchCommand}"
                            Style="{StaticResource SearchBarBtnStyle}"
                            ToolTip="Filter">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FilterRemoveOutline" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>
            <compt:ExpandedDataGrid x:Name="DgQttVoucherDetail"
                                    Grid.Row="3"
                                    Margin="0 3 0 10"
                                    VerticalAlignment="Top"
                                    FrozenColumnCount="6"
                                    AutoGenerateColumns="False"
                                    CanUserResizeColumns="True"
                                    BorderThickness="1"
                                    HorizontalScrollBarVisibility="Auto"
                                    wpf:DataGridAssist.CornerRadius="0 0 4 4"
                                    CanUserAddRows="False"
                                    CanUserDeleteRows="False"
                                    GridLinesVisibility="All"
                                    ScrollViewer.ScrollChanged="dgdData_ScrollChanged"
                                    ColumnInfo="{Binding Columns}"
                                    ElementFrozenColumn="{Binding ElementName=cbFrozenColumn, Path=., Mode=TwoWay}"
                                    ElementDynamicColumn="{Binding ElementName=pbDynamicColumn, Path=., Mode=TwoWay}"
                                    Style="{StaticResource MaterialDesignDataGrid}"
                                    CellStyle="{StaticResource DataGridCellDetail}"
                                    RowStyle="{StaticResource DataGridRowDetail}"
                                    ItemsSource="{Binding Items}"
                                    ext:DataGridBehavior.LastColumnFill="True"
                                    SelectedItem="{Binding SelectedItem}">
                <DataGrid.Resources>
                    <helper:BindingProxy x:Key="DataContextProxy"
                                         Data="{Binding}" />
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn x:Name="STenBhMLNS"
                                        Header="Nội dung"
                                        ElementStyle="{StaticResource DataGridTextColumnDetailStyle}"
                                        Binding="{Binding STenBhMLNS}"
                                        IsReadOnly="True"
                                        Width="Auto" />
                    <DataGridTextColumn Visibility="{Binding Data.IsShowAgencyFilter, Source={StaticResource DataContextProxy}, Converter={StaticResource BooleanToVisibilityConverter}}"
                                        x:Name="Agency"
                                        Header="Đơn vị"
                                        Binding="{Binding STenDonVi}"
                                        IsReadOnly="True"
                                        ElementStyle="{StaticResource DataGridTextColumnDetailStyle}" />
                    <DataGridTextColumn x:Name="QSBQNam"
                                              Header="QSBQ"
                                              IsReadOnly="{Binding Data.IsVoucherSummary, Source={StaticResource DataContextProxy}}"
                                              Binding="{Binding IQSBQNam, Converter={ StaticResource NumberIntToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}" />
                    <compt:DataGridTextColumn x:Name="LuongChinh"
                                              Header="Lương chính"
                                              StringFormat="{}{0:N0}"
                                              IsReadOnly="{Binding Data.IsVoucherSummary, Source={StaticResource DataContextProxy}}"
                                              Binding="{Binding FLuongChinh, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding), Converter={StaticResource HeaderPaddingConverter}}">
                                        <Canvas Height="{Binding ElementName=HeaderGroup, Path=ActualHeight}">
                                            <Grid>
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Width">
                                                            <Setter.Value>
                                                                <MultiBinding Converter="{StaticResource MultipleWidthConverter}">
                                                                    <Binding ElementName="LuongChinh"
                                                                             Path="ActualWidth" />
                                                                    <Binding ElementName="PCChucVu"
                                                                             Path="ActualWidth" />
                                                                    <Binding ElementName="PCTNNghe"
                                                                             Path="ActualWidth" />
                                                                    <Binding ElementName="PCTNVK"
                                                                             Path="ActualWidth" />
                                                                    <Binding ElementName="NghiOm"
                                                                             Path="ActualWidth" />
                                                                    <Binding ElementName="HSBL"
                                                                             Path="ActualWidth" />
                                                                    <Binding ElementName="TongQuyTienLuongNam"
                                                                             Path="ActualWidth" />
                                                                </MultiBinding>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </Grid.Style>
                                                <TextBlock Name="HeaderGroup"
                                                           HorizontalAlignment="Center"
                                                           Text="Quỹ tiền lương (năm) làm căn cứ đóng" />
                                            </Grid>
                                        </Canvas>
                                    </Border>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Text="Lương chính"
                                                   HorizontalAlignment="Center" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                        <DataGridTextColumn.CellStyle>
                            <Style TargetType="DataGridCell"
                                   BasedOn="{StaticResource DataGridCellDetail}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Path=IsCancel}"
                                                 Value="True">
                                        <Setter Property="IsEnabled"
                                                Value="False"></Setter>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.CellStyle>
                    </compt:DataGridTextColumn>
                    <compt:DataGridTextColumn x:Name="PCChucVu"
                                              Header="PC chức vụ"
                                              StringFormat="{}{0:N0}"
                                              IsReadOnly="{Binding Data.IsVoucherSummary, Source={StaticResource DataContextProxy}}"
                                              Binding="{Binding FPhuCapChucVu, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Name="HeaderGroup"
                                                   Text="PC chức vụ" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>
                    <compt:DataGridTextColumn x:Name="PCTNNghe"
                                              Header="PC TN nghề"
                                              StringFormat="{}{0:N0}"
                                              IsReadOnly="{Binding Data.IsVoucherSummary, Source={StaticResource DataContextProxy}}"
                                              Binding="{Binding FPCTNNghe, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />

                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Name="HeaderGroup"
                                                   Text="PC TN nghề" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>
                    <compt:DataGridTextColumn x:Name="PCTNVK"
                                              Header="PC TN VK"
                                              StringFormat="{}{0:N0}"
                                              IsReadOnly="{Binding Data.IsVoucherSummary, Source={StaticResource DataContextProxy}}"
                                              Binding="{Binding FPCTNVuotKhung, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Name="HeaderGroup"
                                                   Text="PC TN VK" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>
                    <compt:DataGridTextColumn x:Name="NghiOm"
                                              Header="Nghỉ ốm dưới 14 ngày / tháng"
                                              StringFormat="{}{0:N0}"
                                              IsReadOnly="{Binding Data.IsVoucherSummary, Source={StaticResource DataContextProxy}}"
                                              Binding="{Binding FNghiOm, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Name="HeaderGroup"
                                                   Text="Nghỉ ốm dưới 14 ngày / tháng" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>
                    <compt:DataGridTextColumn x:Name="HSBL"
                                              Header="HSBL"
                                              StringFormat="{}{0:N0}"
                                              IsReadOnly="{Binding Data.IsVoucherSummary, Source={StaticResource DataContextProxy}}"
                                              Binding="{Binding FHSBL, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Name="HeaderGroup"
                                                   Text="HSBL" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>
                    <compt:DataGridTextColumn x:Name="TongQuyTienLuongNam"
                                              Header="Cộng (Quỹ tiền lương)"
                                              StringFormat="{}{0:N0}"
                                              IsReadOnly="True"
                                              Binding="{Binding FTongQuyTienLuongNam, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border BorderThickness="0 0 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding), Converter={StaticResource HeaderPaddingConverter}}">
                                        <Canvas Height="{Binding ElementName=HeaderGroup, Path=ActualHeight}">
                                        </Canvas>
                                    </Border>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Name="HeaderGroup"
                                                   HorizontalAlignment="Center"
                                                   Text="Cộng" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>

                    <compt:DataGridTextColumn x:Name="DuToan"
                                              Header="Dự toán"
                                              IsReadOnly="True"
                                              Visibility="Collapsed"
                                              Binding="{Binding FDuToan, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}">
                    </compt:DataGridTextColumn>

                    <compt:DataGridTextColumn x:Name="DaQuyetToan"
                                              Header="Đã quyết toán"
                                              IsReadOnly="True"
                                              Visibility="{Binding Data.IsInBudget, Converter={StaticResource BooleanToVisibilityConverter}, Source={StaticResource DataContextProxy}}"
                                              Binding="{Binding FDaQuyetToan, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}">
                    </compt:DataGridTextColumn>

                    <compt:DataGridTextColumn x:Name="ConLai"
                                              Header="Còn lại"
                                              IsReadOnly="True"
                                              Visibility="Collapsed"
                                              Binding="{Binding FConLai, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}">
                    </compt:DataGridTextColumn>

                    <compt:DataGridTextColumn x:Name="NLDDongBHXH"
                                              Header="NLĐ đóng (BHXH)"
                                              StringFormat="{}{0:N0}"
                                              IsReadOnly="True"
                                              Binding="{Binding FThuBHXHNLD, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding), Converter={StaticResource HeaderPaddingConverter}}">
                                        <Canvas Height="{Binding ElementName=HeaderGroup, Path=ActualHeight}">
                                            <Grid>
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Width">
                                                            <Setter.Value>
                                                                <MultiBinding Converter="{StaticResource MultipleWidthConverter}">
                                                                    <Binding ElementName="NLDDongBHXH"
                                                                             Path="ActualWidth" />
                                                                    <Binding ElementName="NSDDongBHXH"
                                                                             Path="ActualWidth" />
                                                                    <Binding ElementName="CongBHXH"
                                                                             Path="ActualWidth" />
                                                                </MultiBinding>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </Grid.Style>
                                                <TextBlock Name="HeaderGroup"
                                                           HorizontalAlignment="Center"
                                                           Text="Số phải thu BHXH" />
                                            </Grid>
                                        </Canvas>
                                    </Border>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Text="NLĐ đóng"
                                                   HorizontalAlignment="Center" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>
                    <compt:DataGridTextColumn x:Name="NSDDongBHXH"
                                              Header="NSD đóng (BHXH)"
                                              StringFormat="{}{0:N0}"
                                              IsReadOnly="True"
                                              Binding="{Binding FThuBHXHNSD, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Name="HeaderGroup"
                                                   Text="NSD đóng" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>
                    <compt:DataGridTextColumn x:Name="CongBHXH"
                                              Header="Cộng (BHXH)"
                                              StringFormat="{}{0:N0}"
                                              IsReadOnly="True"
                                              Binding="{Binding FTongSoPhaiThuBHXH, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border BorderThickness="0 0 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding), Converter={StaticResource HeaderPaddingConverter}}">
                                        <Canvas Height="{Binding ElementName=HeaderGroup, Path=ActualHeight}">
                                        </Canvas>
                                    </Border>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Name="HeaderGroup"
                                                   HorizontalAlignment="Center"
                                                   Text="Cộng" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>

                    <compt:DataGridTextColumn x:Name="NLDDongBHYT"
                                              Header="NLĐ đóng (BHYT)"
                                              StringFormat="{}{0:N0}"
                                              IsReadOnly="True"
                                              Binding="{Binding FThuBHYTNLD, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding), Converter={StaticResource HeaderPaddingConverter}}">
                                        <Canvas Height="{Binding ElementName=HeaderGroup, Path=ActualHeight}">
                                            <Grid>
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Width">
                                                            <Setter.Value>
                                                                <MultiBinding Converter="{StaticResource MultipleWidthConverter}">
                                                                    <Binding ElementName="NLDDongBHYT"
                                                                             Path="ActualWidth" />
                                                                    <Binding ElementName="NSDDongBHYT"
                                                                             Path="ActualWidth" />
                                                                    <Binding ElementName="CongBHYT"
                                                                             Path="ActualWidth" />
                                                                </MultiBinding>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </Grid.Style>
                                                <TextBlock Name="HeaderGroup"
                                                           HorizontalAlignment="Center"
                                                           Text="Số phải thu BHYT" />
                                            </Grid>
                                        </Canvas>
                                    </Border>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Text="NLĐ đóng"
                                                   HorizontalAlignment="Center" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>
                    <compt:DataGridTextColumn x:Name="NSDDongBHYT"
                                              Header="NSD đóng (BHYT)"
                                              StringFormat="{}{0:N0}"
                                              IsReadOnly="True"
                                              Binding="{Binding FThuBHYTNSD, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Name="HeaderGroup"
                                                   Text="NSD đóng" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>
                    <compt:DataGridTextColumn x:Name="CongBHYT"
                                              Header="Cộng (BHYT)"
                                              StringFormat="{}{0:N0}"
                                              IsReadOnly="True"
                                              Binding="{Binding FTongSoPhaiThuBHYT, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border BorderThickness="0 0 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding), Converter={StaticResource HeaderPaddingConverter}}">
                                        <Canvas Height="{Binding ElementName=HeaderGroup, Path=ActualHeight}">
                                        </Canvas>
                                    </Border>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Name="HeaderGroup"
                                                   HorizontalAlignment="Center"
                                                   Text="Cộng" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>

                    <compt:DataGridTextColumn x:Name="NLDDongBHTN"
                                              Header="NLĐ đóng (BHTN)"
                                              StringFormat="{}{0:N0}"
                                              IsReadOnly="True"
                                              Binding="{Binding FThuBHTNNLD, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding), Converter={StaticResource HeaderPaddingConverter}}">
                                        <Canvas Height="{Binding ElementName=HeaderGroup, Path=ActualHeight}">
                                            <Grid>
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Width">
                                                            <Setter.Value>
                                                                <MultiBinding Converter="{StaticResource MultipleWidthConverter}">
                                                                    <Binding ElementName="NLDDongBHTN"
                                                                             Path="ActualWidth" />
                                                                    <Binding ElementName="NSDDongBHTN"
                                                                             Path="ActualWidth" />
                                                                    <Binding ElementName="CongBHTN"
                                                                             Path="ActualWidth" />
                                                                </MultiBinding>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </Grid.Style>
                                                <TextBlock Name="HeaderGroup"
                                                           HorizontalAlignment="Center"
                                                           Text="Số phải thu BHTN" />
                                            </Grid>
                                        </Canvas>
                                    </Border>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Text="NLĐ đóng"
                                                   HorizontalAlignment="Center" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>
                    <compt:DataGridTextColumn x:Name="NSDDongBHTN"
                                              Header="NSD đóng (BHTN)"
                                              StringFormat="{}{0:N0}"
                                              IsReadOnly="True"
                                              Binding="{Binding FThuBHTNNSD, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Name="HeaderGroup"
                                                   Text="NSD đóng" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>
                    <compt:DataGridTextColumn x:Name="CongBHTN"
                                              Header="Cộng (BHTN)"
                                              StringFormat="{}{0:N0}"
                                              IsReadOnly="True"
                                              Binding="{Binding FTongSoPhaiThuBHTN, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border BorderThickness="0 0 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding), Converter={StaticResource HeaderPaddingConverter}}">
                                        <Canvas Height="{Binding ElementName=HeaderGroup, Path=ActualHeight}">
                                        </Canvas>
                                    </Border>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Name="HeaderGroup"
                                                   HorizontalAlignment="Center"
                                                   Text="Cộng" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>

                    <compt:DataGridTextColumn x:Name="TongCong"
                                              Header="Tổng số phải thu"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              Binding="{Binding FTongCong, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              IsReadOnly="True"
                                              Width="140"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}">
                    </compt:DataGridTextColumn>

                    <DataGridTextColumn x:Name="GhiChu"
                                        Header="Ghi chú"
                                        IsReadOnly="{Binding Data.IsVoucherSummary, Source={StaticResource DataContextProxy}}"
                                        ElementStyle="{StaticResource DataGridTextColumnDetailStyle}"
                                        EditingElementStyle="{StaticResource DataGridTextColumnEditingStyle}"
                                        Binding="{Binding SGhiChu, UpdateSourceTrigger=LostFocus}"
                                        Width="Auto" />
                </DataGrid.Columns>
            </compt:ExpandedDataGrid>
            <ScrollViewer Grid.Row="4"
                          x:Name="scrollFooter"
                          HorizontalScrollBarVisibility="Hidden"
                          VerticalScrollBarVisibility="Hidden">
                <StackPanel Grid.Row="4"
                            Orientation="Horizontal">
                    <Grid Width="{Binding ElementName=STenBhMLNS, Path=ActualWidth}" />
                    <Grid Visibility="{Binding IsShowAgencyFilter, Converter={StaticResource BooleanToVisibilityConverter}}"
                          Width="{Binding ElementName=Agency, Path=ActualWidth}" />
                    <Grid Width="{Binding ElementName=QSBQNam, Path=ActualWidth}">
                        <TextBox Text="{Binding Model.IQSBQNam, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=LuongChinh, Path=ActualWidth}">
                        <TextBox Text="{Binding Model.FLuongChinh, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=PCChucVu, Path=ActualWidth}">
                        <TextBox Text="{Binding Model.FPhuCapChucVu, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=PCTNNghe, Path=ActualWidth}">
                        <TextBox Text="{Binding Model.FPCTNNghe, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=PCTNVK, Path=ActualWidth}">
                        <TextBox Text="{Binding Model.FPCTNVuotKhung, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=NghiOm, Path=ActualWidth}">
                        <TextBox Text="{Binding Model.FNghiOm, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=HSBL, Path=ActualWidth}">
                        <TextBox Text="{Binding Model.FHSBL, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=TongQuyTienLuongNam, Path=ActualWidth}">
                        <TextBox Text="{Binding Model.FTongQuyTienLuongNam, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <!--<Grid Width="{Binding ElementName=DuToan,Path=ActualWidth}"
                          Visibility="{Binding IsInBudget, Converter={StaticResource InverseBoolToVisConverter}}">
                        <TextBox Text="{Binding Model.FDuToan, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>-->
                    <!--<Grid Width="{Binding ElementName=DaQuyetToan,Path=ActualWidth}">

                        <TextBox Text="{Binding Model.FDaQuyetToan, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>-->
                    <!--<Grid Width="{Binding ElementName=ConLai,Path=ActualWidth}"
                          Visibility="{Binding IsInBudget, Converter={StaticResource InverseBoolToVisConverter}}">
                        <TextBox Text="{Binding Model.FConLai, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>-->
                    <Grid Width="{Binding ElementName=NLDDongBHXH,Path=ActualWidth}">
                        <TextBox Text="{Binding Model.FThuBHXHNLD, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=NSDDongBHXH,Path=ActualWidth}">
                        <TextBox Text="{Binding Model.FThuBHXHNSD, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=CongBHXH,Path=ActualWidth}">
                        <TextBox Text="{Binding Model.FTongSoPhaiThuBHXH, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=NLDDongBHYT,Path=ActualWidth}">
                        <TextBox Text="{Binding Model.FThuBHYTNLD, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=NSDDongBHYT,Path=ActualWidth}">
                        <TextBox Text="{Binding Model.FThuBHYTNSD, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=CongBHYT,Path=ActualWidth}">
                        <TextBox Text="{Binding Model.FTongSoPhaiThuBHYT, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=NLDDongBHTN,Path=ActualWidth}">
                        <TextBox Text="{Binding Model.FThuBHTNNLD, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=NSDDongBHTN,Path=ActualWidth}">
                        <TextBox Text="{Binding Model.FThuBHTNNSD, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=CongBHTN,Path=ActualWidth}">
                        <TextBox Text="{Binding Model.FTongSoPhaiThuBHTN, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=TongCong,Path=ActualWidth}">
                        <TextBox Text="{Binding Model.FTongCong, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=GhiChu,Path=ActualWidth}">

                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </materialDesign:DialogHost>
</Window>