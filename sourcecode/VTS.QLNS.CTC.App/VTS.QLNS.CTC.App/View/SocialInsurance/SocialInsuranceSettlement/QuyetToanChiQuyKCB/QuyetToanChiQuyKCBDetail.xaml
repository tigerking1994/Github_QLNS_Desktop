<Window x:Class="VTS.QLNS.CTC.App.View.SocialInsurance.SocialInsuranceSettlement.QuyetToanChiQuyKCB.QuyetToanChiQuyKCBDetail"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wpf="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
        xmlns:localEnum="clr-namespace:VTS.QLNS.CTC.Utility.Enum;assembly=VTS.QLNS.CTC.Utility"
        xmlns:extensions="clr-namespace:VTS.QLNS.CTC.App.Extensions"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:compt="clr-namespace:VTS.QLNS.CTC.App.Component"
        xmlns:dc="clr-namespace:VTS.QLNS.CTC.App.ViewModel.SocialInsurance.SocialInsuranceSettlement.QuyetToanChiQuyKCB"
        xmlns:helper="clr-namespace:VTS.QLNS.CTC.App.Helper"
        x:Name="DetailWindow"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Quản lý thu chi bảo hiểm - Quyêt toán chi KCB quân y đơn vị "
        Height="768"
        Width="1280"
        WindowStartupLocation="CenterScreen"
        xmlns:ext="clr-namespace:VTS.QLNS.CTC.App.Extensions"
        WindowState="Maximized"
        Icon="/favicon.ico">
    <Window.InputBindings>
        <!--<KeyBinding Key="F4"
                    Command="{Binding DeleteCommand}"
                    CommandParameter="{Binding ElementName=dgdDataAllocationDetail, Path=.}" />-->
        <KeyBinding Key="F10"
                    Command="{Binding SaveCommand}" />
        <KeyBinding Key="F5"
                    Command="{Binding RefreshCommand}" />
    </Window.InputBindings>
    <Window.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="groupingHeaderTemplate">
                <TextBlock Text="test"
                           Foreground="Navy"
                           FontWeight="Bold"
                           FontSize="12" />
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>
    <materialDesign:DialogHost Identifier="{x:Static localEnum:DemandCheckScreen.DETAIL_DIALOG}"
                               DialogTheme="Inherit">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0"
                        Orientation="Vertical">
                <DockPanel>
                    <materialDesign:PackIcon Kind="Dollar"
                                             Style="{StaticResource DialogScreenIconStyle}" />
                    <StackPanel Orientation="Vertical"
                                VerticalAlignment="Center">
                        <TextBlock Text="CHI TIẾT"
                                   FontWeight="Bold" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="Số: {0} - Ngày: {1} | Đơn vị: {2}">
                                        <Binding Path="Model.SSoChungTu" />
                                        <!--<Binding Path="Model.SSoQuyetDinh" />-->
                                        <Binding Path="Model.DNgayChungTu"
                                                 Converter="{StaticResource DateTimeToStringConverter}" />
                                        <Binding Path="Model.STenDonVi" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Grid>
                    </StackPanel>
                </DockPanel>
                <Separator Background="{DynamicResource MaterialDesignDivider}" />
            </StackPanel>
            <StackPanel Grid.Row="1">
                <StackPanel Grid.Row="1"
                            Orientation="Horizontal">

                    <Button Style="{StaticResource SearchBarBtnStyle}"
                            Command="{Binding SaveCommand}"
                            IsEnabled="{Binding IsSaveData}"
                            ToolTip="Lưu dữ liệu">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="ContentSave" />
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}"
                                       Text="Lưu dữ liệu (F10)" />
                        </StackPanel>
                    </Button>
                    <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                    <Button Style="{StaticResource SearchBarBtnStyle}"
                            ToolTip="Refresh"
                            Command="{Binding RefreshCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Refresh" />
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}"
                                       Text="Tải lại (F5)" />
                        </StackPanel>
                    </Button>
                    <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                    <Button Style="{StaticResource SearchBarBtnStyle}"
                            Command="{Binding RefreshCommand}"
                            ToolTip="Giải thích">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="NoteText" />
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}"
                                       Text="Giải thích" />
                            <materialDesign:PopupBox Margin="-10 0 0 0"
                                                     Grid.Column="1"
                                                     HorizontalAlignment="Right"
                                                     IsPopupOpen="{Binding IsOpenExplainPopup, Mode=TwoWay}">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="MenuDown" />
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel>
                                    <Button Command="{Binding GiaiThichBangLoiCommand}"
                                            Content="Giải thích bằng lời" />
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </Button>
                    <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                    <Button x:Name="btnPrint"
                            Command="{Binding BtnPrintCommand}"
                            Style="{StaticResource SearchBarBtnStyle}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Printer" />
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">In báo cáo</TextBlock>
                            <materialDesign:PopupBox Margin="-10,0,0,0"
                                                     HorizontalAlignment="Right"
                                                     IsPopupOpen="{Binding IsOpenPrintPopup, Mode=TwoWay}">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="MenuDown" />
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel>
                                    <Button Command="{Binding PrintCommand}"
                                            CommandParameter="{x:Static localEnum:BhQuyetToanChiQuyKCBType.PRINT_THONGTRIQUYETTOANCHIKINHPHIKCB}"
                                            Content="Thông tri quyết toán chi kinh phí KCB tại quân y đơn vị" />
                                    <Button Command="{Binding PrintCommand}"
                                            CommandParameter="{x:Static localEnum:BhQuyetToanChiQuyKCBType.PRINT_BAOCAOKCBQUANYDONVI}"
                                            Content="Báo cáo quyết toán chi kinh phí KCB tại quân y đơn vị" />
                                    <Button Command="{Binding PrintCommand}"
                                            CommandParameter="{x:Static localEnum:BhQuyetToanChiQuyKCBType.PRINT_BAOCAOTONGHOPCHI}"
                                            Content="Bảng kê tổng hợp chi KCB tại quân y đơn vị" />
                                    <!--<Button Command="{Binding PrintCommand}" CommandParameter="{x:Static localEnum:BhQuyetToanChiQuyKCBType.PRINT_BAOCAOTONGHOPCCACDONVI}" Content="Báo cáo tổng hợp các đơn vị về chi phí khám, chữa bệnh"/>-->

                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </Button>
                    <StackPanel Orientation="Horizontal"
                                Visibility="{Binding IsTongHop, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                        <ComboBox materialDesign:HintAssist.Hint="-- Cách hiển thị dữ liệu --"
                                  DisplayMemberPath="DisplayItem"
                                  SelectedValuePath="ValueItem"
                                  ItemsSource="{Binding TypeShowAgency}"
                                  SelectedItem="{Binding SelectedTypeShowAgency, UpdateSourceTrigger=PropertyChanged}"
                                  Style="{StaticResource MaterialDesignFloatingHintComboBox}" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal"
                                Visibility="{Binding IsShowAgencyFilter, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                        <ComboBox materialDesign:HintAssist.Hint="-- Đơn vị --"
                                  materialDesign:TextFieldAssist.HasClearButton="True"
                                  DisplayMemberPath="DisplayItem"
                                  SelectedItem="{Binding SelectedAgency}"
                                  ItemsSource="{Binding Agencies}"
                                  Style="{StaticResource SearchBarHorizontalComboBoxStyle}" />
                    </StackPanel>
                    <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                    <Button x:Name="btnShowCol"
                            Style="{StaticResource SearchBarBtnStyle}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="PlusMinusVariant" />
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}"
                                       Text="Ẩn/hiện cột" />
                            <materialDesign:PopupBox Name="pbDynamicColumn"
                                                     Margin="-10 0 0 0"
                                                     HorizontalAlignment="Right"
                                                     StaysOpen="True">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="MenuDown" />
                                </materialDesign:PopupBox.ToggleContent>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </Button>
                    <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                    <ComboBox Name="cbFrozenColumn"
                              materialDesign:HintAssist.Hint="Cố định cột"
                              DisplayMemberPath="DisplayItem"
                              SelectedValuePath="ValueItem"
                              Style="{StaticResource MaterialDesignFloatingHintComboBox}" />
                    <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                    <Button Command="{Binding CloseCommand}"
                            CommandParameter="{Binding ElementName=DetailWindow}"
                            Style="{StaticResource SearchBarBtnStyle}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CloseOutline" />
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}"
                                       Text="Đóng" />
                        </StackPanel>
                    </Button>
                </StackPanel>
                <Separator Background="{DynamicResource MaterialDesignDivider}" />
                <StackPanel  Orientation="Horizontal">
                    <Grid Width="{Binding ElementName=STenBhMLNS, Path=ActualWidth}">
                        <TextBox x:Name="txtSelectedLNS"
                                 Margin="0 0 5 0"
                                 materialDesign:HintAssist.Hint="Loại trợ cấp"
                                 Text="{Binding SNoiDungSearch, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource MaterialDesignFloatingHintTextBox}">
                            <TextBox.InputBindings>
                                <KeyBinding Command="{Binding SearchCommand}"
                                            Key="Return" />
                            </TextBox.InputBindings>
                        </TextBox>
                        <materialDesign:PopupBox Margin="-10 10 0 0"
                                                 Grid.Column="1"
                                                 HorizontalAlignment="Right"
                                                 IsPopupOpen="{Binding IsPopupOpen, Mode=TwoWay}"
                                                 StaysOpen="True">
                            <materialDesign:PopupBox.ToggleContent>
                                <materialDesign:PackIcon Kind="MenuDown" />
                            </materialDesign:PopupBox.ToggleContent>
                            <StackPanel Margin="10"
                                        Orientation="Vertical">
                                <TextBox materialDesign:HintAssist.Hint="Tìm kiếm"
                                         Text="{Binding SearchLNS, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                                <DataGrid Height="450"
                                          Margin="0 12 0 0"
                                          VerticalAlignment="Top"
                                          AutoGenerateColumns="False"
                                          CanUserResizeColumns="True"
                                          BorderThickness="1"
                                          HorizontalScrollBarVisibility="Auto"
                                          SelectedItem="{Binding SelectedPopupItem}"
                                          ItemsSource="{Binding DataPopupSearchItems}"
                                          CanUserAddRows="False"
                                          RowStyle="{StaticResource DataGridRowDetail}">
                                    <DataGrid.Columns>
                                        <!--<DataGridTextColumn Header="LNS" Binding="{Binding Lns}" IsReadOnly="True"/>-->
                                        <DataGridTextColumn Header="Loại trợ cấp"
                                                            Binding="{Binding SNoiDung}"
                                                            IsReadOnly="True" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </StackPanel>
                        </materialDesign:PopupBox>
                    </Grid>
                    <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}" />
                    <ComboBox materialDesign:HintAssist.Hint="-- Cách hiển thị dữ liệu --"
                              DisplayMemberPath="DisplayItem"
                              SelectedValuePath="ValueItem"
                              ItemsSource="{Binding TypeShows}"
                              SelectedItem="{Binding TypeShowsSelected, UpdateSourceTrigger=PropertyChanged}"
                              Style="{StaticResource MaterialDesignFloatingHintComboBox}" />
                    <Separator Style="{StaticResource SearchBarHorizontalDividerStyle}"
                               Margin="0 0 0 0" />
                    <Button Style="{StaticResource SearchBarBtnStyle}"
                            Command="{Binding SearchCommand}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Search" />
                            <TextBlock Style="{StaticResource SearchBarBtnTextStyle}">Tìm kiếm</TextBlock>
                        </StackPanel>
                    </Button>
                    <Button ext:CellFocusExtension.ElementToFocus="{Binding ElementName=txtSelectedLNS}"
                            Command="{Binding ClearSearchCommand}"
                            Style="{StaticResource SearchBarBtnStyle}"
                            ToolTip="Filter">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="FilterRemoveOutline" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>
            <compt:ExpandedDataGrid x:Name="dgdDataQuyKCBDetail"
                                    Grid.Row="3"
                                    Margin="0 3 0 10"
                                    VerticalAlignment="Top"
                                    FrozenColumnCount="6"
                                    AutoGenerateColumns="False"
                                    CanUserResizeColumns="True"
                                    BorderThickness="1"
                                    HorizontalScrollBarVisibility="Auto"
                                    wpf:DataGridAssist.CornerRadius="0 0 4 4"
                                    CanUserAddRows="False"
                                    CanUserDeleteRows="False"
                                    GridLinesVisibility="All"
                                    ScrollViewer.ScrollChanged="dgdData_ScrollChanged"
                                    ColumnInfo="{Binding Columns}"
                                    ElementFrozenColumn="{Binding ElementName=cbFrozenColumn, Path=., Mode=TwoWay}"
                                    ElementDynamicColumn="{Binding ElementName=pbDynamicColumn, Path=., Mode=TwoWay}"
                                    Style="{StaticResource MaterialDesignDataGrid}"
                                    CellStyle="{StaticResource DataGridCellDetail}"
                                    RowStyle="{StaticResource DataGridRowDetail}"
                                    ItemsSource="{Binding Items}"
                                    ext:DataGridBehavior.LastColumnFill="True"
                                    SelectedItem="{Binding SelectedItem}">
                <DataGrid.Resources>
                    <helper:BindingProxy x:Key="DataContextProxy"
                                         Data="{Binding}" />
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn x:Name="STenBhMLNS"
                                        Header="Loại trợ cấp"
                                        ElementStyle="{StaticResource DataGridTextColumnDetailStyle}"
                                        Binding="{Binding SNoiDung}"
                                        IsReadOnly="True"
                                        Width="Auto" />
                    <DataGridTextColumn Visibility="{Binding Data.IsShowColumnUnit, Source={StaticResource DataContextProxy}, Converter={StaticResource BooleanToVisibilityConverter}}"
                                        x:Name="Agency"
                                        Header="Đơn vị"
                                        Binding="{Binding STenDonVi}"
                                        IsReadOnly="True"
                                        ElementStyle="{StaticResource DataGridTextColumnDetailStyle}" />
                    <!--Chỉ tiêu được sử dụng trong năm-->
                    <compt:DataGridTextColumn x:Name="NamTruocChuyenSang"
                                              Header="Năm trước chuyển sang"
                                              Width="200"
                                              IsReadOnly="True"
                                              StringFormat="{}{0:N0}"
                                              Binding="{Binding FTienDuToanNamTruocChuyenSang, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding), Converter={StaticResource HeaderPaddingConverter}}">
                                        <Canvas Height="{Binding ElementName=HeaderGroup, Path=ActualHeight}">
                                            <Grid>
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Width">
                                                            <Setter.Value>
                                                                <MultiBinding Converter="{StaticResource MultipleWidthConverter}">
                                                                    <Binding ElementName="NamTruocChuyenSang"
                                                                             Path="ActualWidth" />
                                                                    <Binding ElementName="ChiTieuDuocGiaoNamNay"
                                                                             Path="ActualWidth" />
                                                                    <Binding ElementName="TongCong"
                                                                             Path="ActualWidth" />
                                                                </MultiBinding>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </Grid.Style>
                                                <TextBlock Name="HeaderGroup"
                                                           HorizontalAlignment="Center"
                                                           Text="Chỉ tiêu được sử dụng trong năm" />
                                            </Grid>
                                        </Canvas>
                                    </Border>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Text="Năm trước chuyển sang"
                                                   HorizontalAlignment="Center" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>

                    <compt:DataGridTextColumn x:Name="ChiTieuDuocGiaoNamNay"
                                              Width="200"
                                              IsReadOnly="True"
                                              Header="Số tiền"
                                              StringFormat="{}{0:N0}"
                                              Binding="{Binding FTienDuToanGiaoNamNay, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <!--<Border
                                        BorderThickness="0 0 1 0"
                                        BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding), Converter={StaticResource HeaderPaddingConverter}}">
                                        <Canvas Height="{Binding ElementName=HeaderGroup, Path=ActualHeight}">
                                        </Canvas>
                                    </Border>-->
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Name="HeaderGroup"
                                                   Text="Chỉ tiêu được giao năm nay" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>

                    <compt:DataGridTextColumn x:Name="TongCong"
                                              Width="200"
                                              Header="Tổng cộng"
                                              StringFormat="{}{0:N0}"
                                              IsReadOnly="True"
                                              Binding="{Binding FTienTongDuToanDuocGiao,Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border BorderThickness="0 0 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding), Converter={StaticResource HeaderPaddingConverter}}">
                                        <Canvas Height="{Binding ElementName=HeaderGroup, Path=ActualHeight}">
                                        </Canvas>
                                    </Border>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Name="HeaderGroup"
                                                   Text="Tổng cộng" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>

                    <!--Lũy kế đến cuối quý này-->
                    <compt:DataGridTextColumn x:Name="SoThucChi"
                                              Width="200"
                                              Header="Số thực chi"
                                              IsReadOnly="True"
                                              Binding="{Binding FTienThucChi, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus,StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding), Converter={StaticResource HeaderPaddingConverter}}">
                                        <Canvas Height="{Binding ElementName=HeaderGroup, Path=ActualHeight}">
                                            <Grid>
                                                <Grid.Style>
                                                    <Style TargetType="Grid">
                                                        <Setter Property="Width">
                                                            <Setter.Value>
                                                                <MultiBinding Converter="{StaticResource MultipleWidthConverter}">
                                                                    <Binding ElementName="SoThucChi"
                                                                             Path="ActualWidth" />
                                                                    <Binding ElementName="SoQuyetToanDaDuocDuyet"
                                                                             Path="ActualWidth" />
                                                                </MultiBinding>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </Grid.Style>
                                                <TextBlock Name="HeaderGroup"
                                                           HorizontalAlignment="Center"
                                                           Text="Lũy kế đến cuối quý" />
                                            </Grid>
                                        </Canvas>
                                    </Border>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Text="Số thực chi"
                                                   HorizontalAlignment="Center" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>
                    <compt:DataGridTextColumn x:Name="SoQuyetToanDaDuocDuyet"
                                              Width="200"
                                              Header="Số quyết toán đã được duyệt"
                                              IsReadOnly="True"
                                              StringFormat="{}{0:N0}"
                                              Binding="{Binding FTienQuyetToanDaDuyet, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                              ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                              EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                              HeaderStyle="{StaticResource DataGridColumnHeaderNoBorder}">
                        <DataGridColumn.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition />
                                        <RowDefinition />
                                    </Grid.RowDefinitions>
                                    <Border BorderThickness="0 0 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding), Converter={StaticResource HeaderPaddingConverter}}">
                                        <Canvas Height="{Binding ElementName=HeaderGroup, Path=ActualHeight}">
                                        </Canvas>
                                    </Border>
                                    <Border Grid.Row="1"
                                            Padding="{Binding RelativeSource={RelativeSource Self}, Path=(materialDesign:DataGridAssist.ColumnHeaderPadding)}"
                                            BorderThickness="0 1 1 0"
                                            BorderBrush="{Binding HorizontalGridLinesBrush, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <TextBlock Name="HeaderGroup"
                                                   Text="Số quyết toán đã được duyệt" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </DataGridColumn.HeaderTemplate>
                    </compt:DataGridTextColumn>

                    <!--Số đề nghị quyết toán quý này-->
                    <DataGridTextColumn x:Name="DeNghiQuyetToanQuyNay"
                                        Header="Số đề nghị quyết toán quý này"
                                        Binding="{Binding FTienDeNghiQuyetToanQuyNay, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                        EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                        ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                        Width="Auto" />

                    <!--Xác nhận quyết toán quý này-->
                    <DataGridTextColumn x:Name="XacNhanQuyetToanQuyNay"
                                        Header="Xác nhận quyết toán quý này"
                                        Binding="{Binding FTienXacNhanQuyetToanQuyNay, Converter={ StaticResource NumberToStringEmptyConverter}, UpdateSourceTrigger=LostFocus, StringFormat={}{0:N0}}"
                                        EditingElementStyle="{StaticResource DataGridTextColumnEditingRightCell}"
                                        ElementStyle="{StaticResource DataGridTextColumnDetailRightStyle}"
                                        Width="Auto" />
                    <DataGridTextColumn x:Name="GhiChu"
                                            Width="200"
                                            Header="Ghi chú"
                                            Binding="{Binding SGhiChu,UpdateSourceTrigger=LostFocus}"
                                            ElementStyle="{StaticResource DataGridTextColumnDetailStyle}"
                                            EditingElementStyle="{StaticResource DataGridTextColumnEditingStyle}" />

                </DataGrid.Columns>
            </compt:ExpandedDataGrid>
            <ScrollViewer Grid.Row="4"
                          x:Name="scrollFooter"
                          HorizontalScrollBarVisibility="Hidden"
                          VerticalScrollBarVisibility="Hidden">
                <StackPanel Grid.Row="4"
                            Orientation="Horizontal">
                    <Grid Width="{Binding ElementName=STenBhMLNS, Path=ActualWidth}"
                          Visibility="{Binding ElementName=STenBhMLNS, Path=Visibility}" />
                    <Grid Width="{Binding ElementName=Agency, Path=ActualWidth}"
                          Visibility="{Binding ElementName=Agency, Path=Visibility}" />

                    <!--Chỉ tiêu được sử dụng trong năm-->
                    <Grid Width="{Binding ElementName=NamTruocChuyenSang,Path=ActualWidth}"
                          Visibility="{Binding ElementName=NamTruocChuyenSang, Path=Visibility}">
                        <TextBox 
                                 Text="{Binding Model.FTongTienDuToanNamTruocChuyenSang, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=ChiTieuDuocGiaoNamNay,Path=ActualWidth}"
                          Visibility="{Binding ElementName=ChiTieuDuocGiaoNamNay, Path=Visibility}">
                        <TextBox 
                                 Text="{Binding Model.FTongTienDuToanGiaoNamNay, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=TongCong,Path=ActualWidth}"
                          Visibility="{Binding ElementName=TongCong, Path=Visibility}">
                        <TextBox 
                                 Text="{Binding Model.FTongTienTongDuToanDuocGiao, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>

                    <!--Lũy kế đến cuối quý này-->
                    <Grid Width="{Binding ElementName=SoThucChi,Path=ActualWidth}"
                          Visibility="{Binding ElementName=SoThucChi, Path=Visibility}">
                        <TextBox 
                                 Text="{Binding Model.FTongTienThucChi, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>
                    <Grid Width="{Binding ElementName=SoQuyetToanDaDuocDuyet,Path=ActualWidth}"
                          Visibility="{Binding ElementName=SoQuyetToanDaDuocDuyet, Path=Visibility}">
                        <TextBox 
                                 Text="{Binding Model.FTongTienQuyetToanDaDuyet, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>

                    <!--Đề nghị quyết toán quý này-->
                    <Grid Width="{Binding ElementName=DeNghiQuyetToanQuyNay,Path=ActualWidth}"
                          Visibility="{Binding ElementName=DeNghiQuyetToanQuyNay, Path=Visibility}">
                        <TextBox 
                                 Text="{Binding Model.FTongTienDeNghiQuyetToanQuyNay, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>

                    <!--Xác nhận quyết toán quý này-->
                    <Grid Width="{Binding ElementName=XacNhanQuyetToanQuyNay,Path=ActualWidth}"
                          Visibility="{Binding ElementName=XacNhanQuyetToanQuyNay, Path=Visibility}">
                        <TextBox 
                                 Text="{Binding Model.FTongTienXacNhanQuyetToanQuyNay, StringFormat={}{0:N0}}"
                                 Style="{StaticResource TextBoxTotalCell}" />
                    </Grid>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </materialDesign:DialogHost>
</Window>